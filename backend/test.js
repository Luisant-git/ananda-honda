const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const topAlpha = await prisma.paymentCollection.findFirst({ orderBy: { receiptNo: 'desc' } }); console.log('Top Alpha:', topAlpha?.receiptNo); const all = await prisma.paymentCollection.findMany({ select: { receiptNo: true } }); const topNum = all.map(p => parseInt(p.receiptNo.replace('RV', ''))).sort((a,b) => b-a)[0]; console.log('Top Num:', topNum); } main().finally(() => prisma.$disconnect());
