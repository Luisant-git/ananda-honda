const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.servicePaymentCollection.count(); console.log('ServicePaymentCount:', count); } main().catch(console.error).finally(() => prisma.$disconnect());
